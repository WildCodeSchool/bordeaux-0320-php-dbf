{{ form_start(form, {'action': path('add_call_process', {callId : call.id})}) }}
<div class="row">
    <div class="col input-field s12">
        {{ form_widget(form.contactType) }}
    </div>
    <div class="col input-field s12">
        {{ form_label(form.comment) }}
        {{ form_widget(form.comment) }}
    </div>


</div>
<button class="btn light-blue">{{ button_label|default('Enregistrer') }} <i class="material-icons right">save</i> </button>
{{ form_end(form) }}
