{{ form_start(form,{'attr': {'id': 'form-transfer' }}, {'action': path('call_transfer_do', {callId : call.id})}) }}
<div class="row">
    <div class="col input-field s12">
        {{ form_widget(form) }}
    </div>
</div>
<div class="row">
    <div class="col s6">
        <input type="submit" value="transférer" class="btn light-blue" id="transfer-call-btn" data-call="{{ call.id }}">
    </div>
    <div class="col s6">
        <div class="progress grey lighten-3 hide" id="transfer-preloader">
            <div class="indeterminate light-blue"></div>
        </div>
    </div>
</div>
{{ form_end(form) }}
