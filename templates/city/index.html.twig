<h1 class="center-align">Plaques</h1>

<table class="table">
    <thead>
    <tr>
        <th>Id</th>
        <th>Plaques</th>
        <th>Modifier</th>
        <th>Supprimer</th>
    </tr>
    </thead>
    <tbody>
    {% for city in cities %}
        <tr id="city-line-{{ city.id }}">
            <td>{{ city.id }}</td>
            <td>{{ city.name }}</td>
            <td>
                <a href="#" class="edit-city-button" data-target="{{ city.id }}"><i class="material-icons">edit</i></a>
            </td>
            <td>
                <a href="#" class="delete-city-button" data-target="{{ city.id }}"> <i class="material-icons">delete</i></a>
            </td>
        </tr>
        <tr class="hide editor-line" id="city-line-edit-{{ city.id }}">
            <td id="city-td-edit-{{ city.id }}" colspan="5"></td>
        </tr>
    {% else %}
        <tr>
            <td colspan="8">no records found</td>
        </tr>
    {% endfor %}
    </tbody>
</table>


