{% block body %}


    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Civilité</th>
                <th>Modifier</th>
                <th>Supprimer</th>
            </tr>
        </thead>
        <tbody>
        {% for civility in civilities %}
            <tr  id="civility-line-{{civility.id}}">
                <td>{{ civility.id }}</td>
                <td>{{ civility.name }}</td>
                <td>
                    <a href="{{ path('civility_show', {'id': civility.id}) }}"><i class="material-icons">edit</i></a>

                </td>
                <td>
                    <a href="#" class="delete-civility-button" data-target="{{civility.id}}" ><i class="material-icons">delete</i></a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}
