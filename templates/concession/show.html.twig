{% extends 'base.html.twig' %}

{% block title %}Concession{% endblock %}

{% block body %}
    <h1>Concession</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ concession.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ concession.name }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ concession.address }}</td>
            </tr>
            <tr>
                <th>Postcode</th>
                <td>{{ concession.postcode }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ concession.city }}</td>
            </tr>
            <tr>
                <th>Brand</th>
                <td>{{ concession.brand }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ concession.phone }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('concession_index') }}">back to list</a>

    <a href="{{ path('concession_edit', {'id': concession.id}) }}">edit</a>

    {{ include('concession/_delete_form.html.twig') }}
{% endblock %}
