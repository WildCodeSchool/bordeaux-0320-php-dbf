<form method="post" action="{{ path('concession_head_delete', {'id': concession_head.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ concession_head.id) }}">
    <button class="btn">Delete</button>
</form>
