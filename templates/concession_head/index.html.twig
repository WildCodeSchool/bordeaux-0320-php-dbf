{% extends 'base.html.twig' %}

{% block title %}ConcessionHead index{% endblock %}

{% block body %}
    <h1>ConcessionHead index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for concession_head in concession_heads %}
            <tr>
                <td>{{ concession_head.id }}</td>
                <td>
                    <a href="{{ path('concession_head_show', {'id': concession_head.id}) }}">show</a>
                    <a href="{{ path('concession_head_edit', {'id': concession_head.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('concession_head_new') }}">Create new</a>
{% endblock %}
