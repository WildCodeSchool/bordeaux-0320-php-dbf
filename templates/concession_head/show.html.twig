{% extends 'base.html.twig' %}

{% block title %}ConcessionHead{% endblock %}

{% block body %}
    <h1>ConcessionHead</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ concession_head.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('concession_head_index') }}">back to list</a>

    <a href="{{ path('concession_head_edit', {'id': concession_head.id}) }}">edit</a>

    {{ include('concession_head/_delete_form.html.twig') }}
{% endblock %}
