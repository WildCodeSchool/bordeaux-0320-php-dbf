<form method="post" action="{{ path('service_head_delete', {'id': service_head.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ service_head.id) }}">
    <button class="btn mgt15 light-blue darken-1"><i class="material-icons">delete</i></button>
</form>
